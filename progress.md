# 传家宝 · 进度交接 / 晨报

> 给「下一轮 AI / 当当回来验收」看的干净交接，不是流水账（日记在 Obsidian `06_迭代日志/开发日记.md`）。
> 每轮 agent 收尾必须更新这一页。配套：规则看 CLAUDE.md，任务状态看 feature_list.json，开工前先跑 ./init.sh。

---

## 最后更新

- 时间：2026-06-20
- 更新人：Claude（抢救代码 + 搭 harness 四件套）

## 现在是什么状态（一句话）

代码已从废纸篓救回到 `/Users/dang/Desktop/传家宝`，GitHub 已同步，harness 四件套已建好。
**还没开始重写**——下一步是当当确认 MVP 主线，然后从 `new-skeleton` 任务开干。

## 上一轮做了什么（2026-06-20）

1. **抢救代码**：真正的本地仓库原本在废纸篓 `/.Trash/Projects/heirloom-tool`（差点被 macOS 自动清空），已搬到 `/Users/dang/Desktop/传家宝`，git 历史完整。
2. **推 GitHub**：原本本地领先 GitHub 2 笔（`b944a48` 部署版、`596d88f` 自己版升级），已 `git push`，`main` 现与远端同步。确认只跟踪 `.env.example`，无密钥泄露。
3. **搭 harness 四件套**：`CLAUDE.md` / `init.sh` / `feature_list.json` / `progress.md`，已把"重写策略 + 要移植的四样器官 + 商业模型 + MVP 主线"写进去。

## 关键结论（重写决策 + 产品方向）

- **重写，不是改造**：旧代码是无状态单页录音卡，掰成新产品比重写还累。
- 但**不清空**：移植四样器官——问题库(Obsidian)、AI 提示词、转写+LLM 管道、部署基建。
- **产品方向已转向**（依据开发日记 2026-04-18 复盘，比 5/28 商业策略更根本）：传家宝是**关系产品不是采访工具**。MVP 脊梁 = 让开不了口的对话被打开 → **低压陪聊**（具体触发物切入、分次5-15分钟，不叫"采访"）→ 转写 → AI 整理出**动人的第一份成品**（人物小传 + 家族线索卡 + 这些故事怎样塑造了我三层）。**命理/八字/家脉多系统收敛全部后置**，不进 MVP。
- "8800 七次结构化访谈课"是成熟期卖法，**不是 MVP 要建的东西**。

## ⚠️ 服务器比本地新（重要）

- 2026-06-03 服务器上 `chuanjiabao.vip` 已上正式 HTTPS（Let's Encrypt 到 2026-09-01），ICP 备案过（蜀ICP备2026029077号，主体成都当前文化传播有限公司），架构改成 **nginx(80/443)→3001、edge-proxy 挪 8080**，`index.html` 4 处链接也改了。
- 救回来的**本地是 5/29 版，落后这些改动**。**重写前先登服务器对账拉回**（任务 `sync-from-server`，只读不动服务器）。

## 下一轮从哪接（按顺序）

1. ⏳ **等当当**：确认 MVP 主线（任务 `mvp-spine-confirm`）——选哪个入口打头（建议先录自己/陪父母）、第一份成品做几层、成品形态。
2. 可并行（不需等当当）：`sync-from-server`，登服务器把最新 index.html / nginx 拉回参照。
3. 当当确认后：`new-skeleton` 转 `in_progress` 搭骨架 → 导题库 → 移植管道 → 跑通一条陪聊流程 → 三层成品 → 部署。

## 其它债（已登记任务）

- 旧 `/api/export` 无鉴权、服务器没配 OSS key、multer 1.x 有告警，重写时一并解决。

## 健康状态

- 本地：`./init.sh` 跑一遍看（重写阶段 server 可能没起，正常）
- 线上：`https://chuanjiabao.vip` 正式入口；pm2 `heirloom` 端口 3001，登服务器 `pm2 list` 确认
